version: "3.8"
services:
  web-app:
    build:
      context: .
      dockerfile: .docker/Dockerfile
    ports:
      - "${PORT:-3500}:3000"
    environment:
      NODE_ENV: production
      NUXT_PUBLIC_API_HOST: "${NUXT_PUBLIC_API_HOST:-}"
    restart: always